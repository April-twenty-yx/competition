import axios from 'axios'

export function topbanner (config) {
  const instance = axios.create({
    baseURL: '#',
    timeout: 8000
  })
  return instance(config)
}

export function midfirpart (config) {
  const instance = axios.create({
    baseURL: '#',
    timeout: 8000
  })
  return instance(config)
}

export function midtwopart (config) {
  const instance = axios.create({
    baseURL: '#',
    timeout: 8000
  })
  return instance(config)
}

export function midthrpart (config) {
  const instance = axios.create({
    baseURL: '#',
    timeout: 8000
  })
  return instance(config)
}

export function midfoupart (config) {
  const instance = axios.create({
    baseURL: '#',
    timeout: 8000
  })
  return instance(config)
}
